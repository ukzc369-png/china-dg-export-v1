import type { Lang, ProductDetailModel } from "../../productDetails";

export function SpecificationTable({ detail, lang }: { detail: ProductDetailModel; lang: Lang }) {
  return <section className="pd-section"><div className="container pd-panel"><p className="pd-kicker">{lang === "en" ? "Technical Data" : "技术数据"}</p><h2>{lang === "en" ? "Technical Specifications" : "技术规格"}</h2><div className="pd-spec-layout"><div className="pd-spec-table"><div className="pd-table-head"><span>{lang === "en" ? "Parameter" : "参数"}</span><strong>{lang === "en" ? "Specification" : "规格"}</strong><em>{lang === "en" ? "Confirmation" : "确认方式"}</em></div>{detail.specifications.map((item) => <div key={item.label.en}><span>{item.label[lang]}</span><strong>{item.value[lang]}</strong><em>{lang === "en" ? "Batch / order review" : "批次 / 订单审核"}</em></div>)}</div><aside className="pd-quality-card"><b aria-hidden="true">♙</b><h3>{lang === "en" ? "Quality Review" : "质量审核"}</h3><p>{lang === "en" ? "Specification and documents are checked against the selected product and shipment." : "根据选定产品和具体出运核对规格与文件。"}</p><ul><li>{lang === "en" ? "COA confirmation" : "COA 确认"}</li><li>{lang === "en" ? "Batch document review" : "批次文件审核"}</li><li>{lang === "en" ? "Order requirement check" : "订单要求核对"}</li></ul></aside></div><p className="pd-footnote">{lang === "en" ? "Reference information only. The final specification and batch COA govern each order." : "以上仅供参考，每笔订单以最终规格和批次 COA 为准。"}</p></div></section>;
}
